const asyncHandler = require("express-async-handler");
const Notifications = require("../../models/Notifications");
const User = require("../../models/User");

const createCustomNotification = asyncHandler(async (req,res)=>{
    const {} = req.body;
});